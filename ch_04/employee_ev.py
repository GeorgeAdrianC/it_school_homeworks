from audioop import mul


emp_val = float(input("Please enter your rating: "))

if 0.00 <= rating <0.4 :
    print("Unacceptable performance")
elif 0.4<=emp_val=>0.6

if rating < 0.0:
    multiplication_factor =0.0
    performance = "invalid rating"
elif 0.0 <= rating < 0.4:
    multiplication_factor =0.0
    performance = "invalid rating"
elif 0.4 <= rating < 0.6:
    multiplication_factor =0.0
    performance = "acceptable rating"